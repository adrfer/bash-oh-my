#!/usr/bin/env bash

# For more info about bash, open a terminal and run:
#
#    man bash
# 
# /!\ Make your own customizations in here

# Show local weather
weather() {
  [[ "${#}" = 0 ]] || {
    echo 'Usage: weather'
    return 1
  }
  
  curl http://wttr.in
}

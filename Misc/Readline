# For more info about `readline`, open a terminal and run this command:
#
#   man 1 bash
#   /^READLINE
#
# Readline is customized by putting commands in the ~/.inputrc.
#
# Use C-x C-r to re-read ~/.inputrc for changes to take place.

# Perform file name matching and completion in a case-insensitive fashion
set completion-ignore-case on

# List file name matching and completion after pressing tab just once
set show-all-if-ambiguous on

# Treat hyphens and underscores as equivalent when performing file name matching and completion
set completion-map-case on

# Replace common prefixes with an ellipsis when displaying possible file name matching and completion
set completion-prefix-display-length 2

# Append a trailing slash when autocompleting symlinks to directories
set mark-symlinked-directories on

# Enable typing a command and use up/down arrow keys to autocomplete from commands in history
"\e[A": history-search-backward
"\e[B": history-search-forward

# Kill whole line regardless of the cursor position
"\C-d": kill-whole-line

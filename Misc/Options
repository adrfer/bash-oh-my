# For more info about shopt, open a terminal and run this command:
#
#   man 1 bash
#   /^ *shopt \[

# Correct minor spelling errors of a directory component in a cd command
shopt -s cdspell

# Match filenames in a case-insensitive fashion when performing file name expansion
shopt -s nocaseglob

# Include file names beginning with a . in the results of their expansion
shopt -s dotglob

# Check the window size after each command and updates the values of LINES and COLUMNS
shopt -s checkwinsize

# Append the history list to the file named by the value of the HISTFILE when the shell exits
shopt -s histappend

# Puts the command to be executed after a substitution on command line as if one had typed it
shopt -s histverify

# Puts a failed history substitution back on the command line for re-editing
shopt -s histreedit
